jdk:
  - openjdk17

before_install:
  - sdk install gradle 8.9 || true
  - sdk use gradle 8.9 || true
  - yes | sdkmanager --licenses >/dev/null 2>&1 || true
  - sdkmanager "platforms;android-35" "build-tools;35.0.0" "platform-tools" || true

install:
  - bash peopleos-apk/jitpack-build.sh
